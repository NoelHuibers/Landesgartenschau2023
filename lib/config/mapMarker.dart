import 'package:flutter/material.dart';
import 'package:latlng/latlng.dart';

class mapMarker {
  const mapMarker({
    required this.image, //Logo für die Markierung gemeint!!!
    required this.titel,
    required this.koordinaten,
  });

  final String image;
  final String titel;
  final LatLng koordinaten;
}

//Hier kommen die Koordianten die einzelnen Stände
final Koordinaten = [
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
// LatLng(latitude, longitude),
];

//Hier werden informationen die einzelnen Stände eingegeben

final mapMarkers = [
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[0]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[1]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[2]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[3]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[4]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[5]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[6]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[7]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[8]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[9]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[10]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[11]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[12]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[13]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[14]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[15]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[16]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[17]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[18]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[19]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[20]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[21]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[22]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[23]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[24]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[25]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[26]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[27]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[28]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[29]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[30]),
  // mapMarker(image: image, titel: titel, koordinaten: Koordinaten[31]),
];
